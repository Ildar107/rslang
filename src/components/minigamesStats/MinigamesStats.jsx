import { useEffect, useState } from 'react';
import getStatistics from '../../services/user.statistic.services';

const MinigamesStats = () => {
  const [stats, setStats] = useState({});



};
